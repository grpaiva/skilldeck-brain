---
title: "Evidence and evaluation"
description: "What counts as evidence, how a meeting becomes one, and how evaluation agents turn it into scores."
icon: "file-search"
updated: "2026-09-01"
status: "verified"
---

## What evidence is

Evidence is a piece of real work that Skill Deck analyzes. It is the foundation of the entire product claim: we evaluate what people did, not what they or anyone else says about it.

Evidence volume is something a manager is expected to watch — "What got evaluated?" opens the Weekly coaching meeting.

The live claim is **real work evidence**. See [Messaging and claims](/playbooks/messaging-and-claims) for the claims that are off limits.

## Where evidence comes from

Evidence has **two** sources.

<Columns cols={2}>
  <Card title="Meeting bots" icon="video">
    A transcript-only bot attends the meeting on **Google Meet**, **Zoom**, or **Microsoft Teams**. This is the first-run source and the one we sell on.
  </Card>

  <Card title="Slack" icon="message-square">
    For the channels the user selects, our bot reads messages daily and evaluates them. Implemented, not first-run.
  </Card>
</Columns>

### The connections that feed them

<AccordionGroup>
  <Accordion title="Google Calendar or Microsoft Calendar" icon="calendar">
    Connecting a calendar is what makes bots join **automatically**. Skill Deck reads upcoming events, and schedules a bot for any event whose meeting link points at Google Meet, Zoom, or Teams.

    The calendar's job is deciding which meetings get a bot.
  </Accordion>

  <Accordion title="Zoom" icon="video">
    Zoom needs **its own connection** on top of the calendar. Once connected, the bot joins as the user, which is what lets a scheduled Zoom meeting be covered.

    The product emails users who have upcoming Zoom meetings and no Zoom connection. Confirm the connection before a pilot with a Zoom-first team.
  </Accordion>

  <Accordion title="Slack" icon="message-square">
    A custom integration with a second step: after connecting the workspace, the user **picks the channels** to track. Channel selection keeps the scope to what the user chose to share.

    A daily sync then reads new messages in each selected channel and turns them into evidence, which goes through the same triage and evaluation as a meeting.
  </Accordion>
</AccordionGroup>

<Note>
  Do not ask new customers or test teams to connect Slack at the start. Slack evaluations tend to score lower than meetings. Other chat platforms, such as Teams chat, are on hold for the same reason.
</Note>

## From meeting to evidence

<Steps>
  <Step title="A bot gets scheduled">
    A connected calendar schedules a **transcript-only** bot for any upcoming event whose link points at Google Meet, Zoom, or Teams. Zoom also needs its own connection, so the bot can join as the user.
  </Step>
  <Step title="The bot joins">
    The bot's progress is visible in the evidence status: `waiting_for_meeting`, `bot_joining`, `bot_in_waiting_room`, then `recording` — shown to the user as **In Meeting**.

    A bot sitting in a waiting room is a state the user can see.
  </Step>
  <Step title="Attendee returns the transcript">
    [Attendee](/overview/glossary) transcribes the meeting and maps speakers. Skill Deck receives text that already says who said what.
  </Step>
  <Step title="Processing">
    The transcript is processed into evidence, then triage and evaluation run.
  </Step>
  <Step title="Triage">
    A triage agent reads the evidence and decides **which Skills it can say anything about**. A one-hour meeting speaks to a handful of Skills, not to all 58.

    Whitelisted Skills get a mild bias **only when the person has no active Track**. Once a Track is active, triage runs unbiased, so the tracked Skill's live score keeps reflecting what the evidence shows. See [The manager role](/product/manager-role).
  </Step>
  <Step title="Evaluation">
    For each Skill triage selected, an evaluator scores the observed behavior against that Skill's level scale, with a written justification. Near-zero scores pass through the [silence gate](#the-gate-silence-is-not-a-score) before they are kept.
  </Step>
  <Step title="Scores update">
    Status reaches `evaluated`. Scores and confidence move for every Skill that was actually scored.
  </Step>
  <Step title="Track analysis and moments, if a Track is active">
    If Tracks are on and the person has an **active Track**, a second job reads the same meeting through [that Skill](/product/cycles-and-tracks). It writes a **Track analysis** — a short Skill-focused summary — and up to **5 moments** (demonstration or opportunity). Each moment is an excerpt plus commentary, capped at **500 characters**, and may be attributed to one [resource card](/product/cycles-and-tracks) by name, never by score.

    The job prefers an empty moments list over stretching a weak connection. It needs the transcript still in cache. If there is no active Track, this step is skipped.

    Starting a Track also backfills analyses for evaluated meetings from the last **7 days**.
  </Step>
  <Step title="Evidence Evaluated email">
    After analysis settles — or immediately after evaluation when there is no active Track — a meeting with evaluations sends the [Evidence Evaluated](/product/track-dispatches) dispatch. That email is assembled from stored analysis and scores.
  </Step>
</Steps>

## The gate: silence is not a score

This is the most important design decision in the evaluation pipeline, and it is a strong point in a demo.

When an evaluator returns a near-zero score, a separate gate agent inspects the justification and decides whether it should be recorded at all. The rule it applies:

- If the justification says the evidence contained **no clear, direct, relevant behavior** for that Skill, the evaluation is discarded.
- If it describes **relevant behavior, even very poor behavior**, the evaluation is kept.

A person is not marked down for a Skill that simply did not come up. A quiet meeting is not a bad score — it produces no score. Genuinely poor behavior does get recorded, including a zero.

This is what stops the profile drifting toward the mean as evidence accumulates, and it is the honest answer to "does it penalize me for meetings where I just listened?"

## What an evaluation contains

Each evaluation carries a score against the Skill's level scale and a written justification tied to what was actually observed.

That is the Deck-facing output. The Track-facing output is separate and only runs on an **active Track**.

## Track analysis and moments

When a Track is active, every newly evaluated meeting is read a second time through the tracked Skill.

| Piece | What it is |
| --- | --- |
| **Track analysis** | One Skill-focused summary for that meeting. Unique per Track + evidence pair. |
| **Moment** | A short excerpt plus commentary. Type is **demonstration** (they showed the behavior) or **opportunity** (they could have and did not). |
| **Card attribution** | Optional. The model may attach a moment to one card in the hand. Scores stay internal. |

Caps: **5 moments** per meeting, **500 characters** per excerpt. Empty moments are allowed, and preferred, when the meeting did not really speak to the Skill.

This is the material in the member's Evidence Evaluated email, the Weekly Review, and a manager's coaching deep-dive. A manager sees the analysis, not a click-through to the meeting. See [Privacy and data](/product/privacy-and-data).

## Asking questions about your evidence

The product includes agents for generating questions about a piece of evidence and answering them, so a user can interrogate their own analysis rather than only receive it. This powers the "Deep Dive" experience on evidence details.

{/* TODO: Confirm whether the evidence Deep Dive is available to managers on a member's evidence, or only to the person the evidence belongs to. */}
